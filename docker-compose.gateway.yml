services:
  gateway:
    image: nginx:1.27-alpine
    container_name: lms_api_gateway
    depends_on:
      - api
    volumes:
      - ./ops/gateway/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "${GATEWAY_PORT:-8080}:8080"
    restart: unless-stopped
