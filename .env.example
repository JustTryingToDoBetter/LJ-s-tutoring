# Copy to `.env` for local development:
#   cp .env.example .env
#
# NOTE:
# - This file is safe to commit (placeholders only).
# - `.env` must never be committed (it is gitignored).

# WhatsApp Configuration (public, country code + number, no "+")
PUBLIC_WHATSAPP_NUMBER=27679327754

# Formspree Configuration (public endpoint)
PUBLIC_FORMSPREE_ENDPOINT=https://formspree.io/f/XXXXX

# Contact Email (public)
PUBLIC_CONTACT_EMAIL=your-email@example.com

# Countdown Configuration (public, ISO 8601)
PUBLIC_COUNTDOWN_DATE=2026-02-15T17:00:00

# Optional: frontend error monitoring (public, leave blank to disable)
PUBLIC_ERROR_MONITOR_ENDPOINT=
PUBLIC_ERROR_MONITOR_SAMPLE_RATE=1

# Public LMS/API base URL (used by admin/tutor/student portals)
PUBLIC_PO_API_BASE=https://api.projectodysseus.live

# Portal subdomains (used for role-based auth redirects)
ADMIN_PORTAL_URL=https://admin.projectodysseus.live
TUTOR_PORTAL_URL=https://tutor.projectodysseus.live
STUDENT_PORTAL_URL=https://student.projectodysseus.live

# ------------------------------
# Server-only secrets (never public)
# ------------------------------
# These values must never be injected into client-side JS.

# ------------------------------
# Tutor Records API (lms-api)
# ------------------------------
# Required by docker-compose for the Postgres container and API DB URL.
# Use a strong unique value in production.
POSTGRES_PASSWORD=postgres

# Database connection
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lms

# Magic link + cookies
PUBLIC_BASE_URL=http://localhost:3001
# In production, set to your Railway app URL, e.g., https://api-projectodysseus.live.up.railway.app or custom domain
COOKIE_SECRET=change_me_to_a_long_random_string
# Comma-separated browser origins allowed to call lms-api
CORS_ORIGIN=http://localhost:8080,http://127.0.0.1:8080,http://localhost:3000,http://127.0.0.1:3000,https://admin.projectodysseus.live,https://tutor.projectodysseus.live,https://student.projectodysseus.live
# In production, keep only your real frontend subdomains and remove local origins.

# Email provider (magic links)
EMAIL_PROVIDER_KEY=
# Set to your email service API key, e.g., Resend API key
EMAIL_FROM=projectodysseus10@gmail.com


