# Copy to `.env` for local development:
#   cp .env.example .env
#
# NOTE:
# - This file is safe to commit (placeholders only).
# - `.env` must never be committed (it is gitignored).

# WhatsApp Configuration (public, country code + number, no "+")
WHATSAPP_NUMBER=27679327754

# Formspree Configuration (public endpoint)
FORMSPREE_ENDPOINT=https://formspree.io/f/XXXXX

# Contact Email (public)
CONTACT_EMAIL=your-email@example.com

# Countdown Configuration (public, ISO 8601)
COUNTDOWN_DATE=2026-02-15T17:00:00

# Optional: frontend error monitoring (public, leave blank to disable)
ERROR_MONITOR_ENDPOINT=
ERROR_MONITOR_SAMPLE_RATE=1

# Public LMS/API base URL (used by admin/tutor portals)
PO_API_BASE=https://api.projectodysseus.live

# ------------------------------
# Server-only secrets (never public)
# ------------------------------
# These values must never be injected into client-side JS.

# ------------------------------
# Tutor Records API (lms-api)
# ------------------------------
# Database connection
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lms

# Magic link + cookies
PUBLIC_BASE_URL=http://localhost:3001
COOKIE_SECRET=change_me_to_a_long_random_string

# Email provider (magic links)
EMAIL_PROVIDER_KEY=
EMAIL_FROM=projectodysseus10@gmail.com

# Arcade monetization + analytics
ARCADE_SESSION_SECRET=change_me_to_a_long_random_string
ARCADE_AD_ALLOWLIST=house
ARCADE_AD_MAX_KB=256
ARCADE_AD_MAX_LOAD_MS=2500
